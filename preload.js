// Preload script для безопасного взаимодействия между renderer и main process
// В текущей версии не требуется дополнительной функциональности

window.addEventListener('DOMContentLoaded', () => {
  console.log('Cosmic Lab Mission загружена');
});
